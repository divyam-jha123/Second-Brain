name: Frontend CI

on:
  pull_request:
    paths:
      - "brainly-frontend/**"
      - ".github/workflows/frontend-ci.yml"
  push:
    branches:
      - main
    paths:
      - "brainly-frontend/**"
      - ".github/workflows/frontend-ci.yml"

concurrency:
  group: frontend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-build-lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: brainly-frontend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: brainly-frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Tests (if configured)
        run: |
          if node -e "process.exit(require('./package.json')?.scripts?.['test:ci'] ? 0 : 1)"; then
            npm run test:ci
          elif node -e "process.exit(require('./package.json')?.scripts?.test ? 0 : 1)"; then
            npm test
          else
            echo "No tests configured for frontend (missing test or test:ci script)."
          fi
